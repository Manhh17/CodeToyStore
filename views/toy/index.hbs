<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Toy Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        .navbar-custom {
            background-color: #343a40;
        }

        .navbar-custom .navbar-nav .nav-link {
            color: #f8f9fa;
        }

        .navbar-custom .navbar-nav .nav-link:hover {
            color: #adb5bd;
        }

        .footer-custom {
            background-color: #343a40;
            color: #f8f9fa;
            padding: 1rem 0;
        }

        img {
            max-width: 100px;
            max-height: 120px;
        }

        .table-warning {
            background-color: #fff3cd;
        }

        .table-light {
            background-color: #f8f9fa;
        }
    </style>
</head>

<body>


    <!-- Main content -->
    <div class="container">
        <h1 class="mb-4">Toy List</h1>

        <!-- Buttons to add or delete toys -->
        <a class="btn btn-info mb-4" href="/toy/add">Add New Toy</a>
        <a class="btn btn-danger mb-4" href="/toy/deleteall">Delete All Toys</a>

        <!-- Toy list table -->
        <div class="table-responsive">
            <table class="table table-striped table-hover table-borderless align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Toy Name</th>
                        <th>Description</th>
                        <th>Price</th>
                        <th>Brand</th>
                        <th>Image</th>
                        <th colspan="2">Menu</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each toys}}
                    <tr class="table-warning">
                        <td>{{name}}</td>
                        <td>{{description}}</td>
                        <td>{{price}}</td>
                        <td>{{brand.name}}</td>
                        <td><img src="{{image}}" alt="Toy Image"></td>
                        <td>
                            <a class="btn btn-warning" href="/toy/edit/{{_id}}">Edit</a>
                        </td>
                        <td>
                            <a class="btn btn-danger" href="/toy/delete/{{_id}}"
                                onclick="return confirm('Are you sure you want to delete this toy?');">Delete</a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>


    <!-- Bootstrap JS, Popper.js, and jQuery -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-9aIt2nRpCQb6l/6n7pJ4w9F5AVds4P7hBereFUVmUMbD5AbmDh20aK3wXlr/3en5"
        crossorigin="anonymous"></script>
</body>

</html>
